apiVersion: batch/v1
kind: Job
metadata:
  name: hello-world
spec:
  template:
    spec:
      containers:
        - name: hello-world
          image: laptop-fedora.tail37fe80.ts.net/hello-world:latest
      imagePullSecrets:
        - name: registry
      restartPolicy: Never
