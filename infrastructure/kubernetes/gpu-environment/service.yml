apiVersion: v1
kind: Service
metadata:
  name: ${CI_PROJECT_NAME}-service
  labels:
    app: ${CI_PROJECT_NAME}
    component: tensorboard
spec:
  type: NodePort
  ports:
    - port: 6006
      targetPort: 6006
  selector:
    app: ${CI_PROJECT_NAME}
    component: tensorboard
